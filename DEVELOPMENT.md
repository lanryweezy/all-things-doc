# Commands to run for local development:
# 
# Terminal 1 (Backend):
# cd backend && python -m uvicorn main:app --reload
#
# Terminal 2 (Frontend):
# npm run dev
#
# Make sure you have set the VITE_BACKEND_URL environment variable to http://localhost:8000